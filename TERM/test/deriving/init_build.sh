#!/bin/bash

source ../default/init_build.sh
# source ../../../../ableC/extensions/string/init_build.sh # Included as an option, so already included in default

export ABLEC_SOURCE+=" $ADT_PATH/deriving/*/*"
